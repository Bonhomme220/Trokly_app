# ── trokly.whatspay.africa — Next.js (PM2 :3001) ──────────────
server {
    listen 80;
    server_name trokly.whatspay.africa;

    # Redirect HTTP → HTTPS (décommente après certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Fichiers statiques Next.js (_next/static)
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_cache_bypass $http_upgrade;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}

# ── HTTPS (décommenté par certbot automatiquement) ──────────────
# server {
#     listen 443 ssl;
#     server_name trokly.whatspay.africa;
#     ssl_certificate     /etc/letsencrypt/live/trokly.whatspay.africa/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/trokly.whatspay.africa/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ... (même location blocks)
# }
